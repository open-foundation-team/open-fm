import { PlayerControls, Sidebar } from "./components";

const App = () => {

  return (
    <main className="app-container">
      <Sidebar />
      <PlayerControls />
    </main>
  );
};

export default App;
